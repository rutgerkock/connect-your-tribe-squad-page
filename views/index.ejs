<%- include('./partials/head') %>

<ul>
  <% squads.forEach(squad => { %>
      <li>
      <a href="/squad/<%= squad.id%>">
          <h2><%= squad.name %> </h2>
      </a>
      </li>
  <% }) %>
  </ul>

<%- include('./partials/foot') %>