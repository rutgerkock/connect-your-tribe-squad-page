<%- include('./partials/head') %>

<link rel="stylesheet" href="/styles.css">
<a id="nav_container_back" href="/">Back</a>

<main>
  <p> <%= "Zoekresulaten voor \'" + search + "\':" %> </p>
  <ul>
      <% persons.forEach(person => { %>
        <li>
          <p>
            <%= person.name + " " + person.surname %>
          </p>
          <p>
            <%= person.github_handle %>
          </p>
          <br>
          <br>
        </li>
      <% }) %>
    </ul>
  </main>